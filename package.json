{
  "name": "miportfolio",
  "private": true,
  "version": "2.0.0",
  "description": "Portfolio React + CMS Electron",
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "build:web": "npm run build --prefix web",
    "dev:admin": "npm run dev --prefix admin",
    "build:admin": "npm run build --prefix admin"
  }
}
